---
phase: LT-01-secure-electron-walking-skeleton
plan: "02"
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - tests/unit/toolchain-smoke.test.ts
autonomous: true
requirements:
  - LT-SEC-001
  - LT-LIC-001
  - LT-PRV-001
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Only the exact human-approved Phase 1 package identities and versions enter the manifest and lockfile."
    - "Node 24, strict TypeScript, and the minimal Vitest smoke path work from a clean install."
    - "Unit tests are one-shot, deterministic, and sampled against the sub-60-second feedback requirement."
  artifacts:
    - path: package.json
      provides: "Private exact dependency boundary and stable typecheck/test:unit scripts"
      contains: "vitest run"
    - path: package-lock.json
      provides: "Reproducible graph derived only after package approval"
    - path: tsconfig.json
      provides: "Strict TypeScript compiler contract"
    - path: vitest.config.ts
      provides: "Bounded one-shot unit-test configuration"
    - path: tests/unit/toolchain-smoke.test.ts
      provides: "RED/GREEN ESM and test-runner tracer"
  key_links:
    - from: 01-01-SUMMARY.md
      to: package.json
      via: "blocking precondition before the first npm install"
      pattern: "approved phase1 packages"
    - from: package.json
      to: package-lock.json
      via: "exact-version npm install after approval"
      pattern: "lockfileVersion"
    - from: package.json
      to: vitest.config.ts
      via: "test:unit invokes vitest run"
      pattern: "test:unit"
---

<objective>
Establish the exact Node 24 manifest, lockfile, strict TypeScript baseline, and minimal TDD runner.

Purpose: Make the first installed byte reviewable and every later behavior slice start from a reproducible one-shot test seam.
Output: Exact private manifest/lockfile, strict TypeScript configuration, bounded Vitest configuration, and a passing smoke test.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/DEVELOPMENT.md
@docs/TESTING.md
@docs/TEST_DATA.md
@docs/LICENSING.md
@.planning/phases/LT-01-secure-electron-walking-skeleton/01-RESEARCH.md
@.planning/phases/LT-01-secure-electron-walking-skeleton/01-VALIDATION.md
@.planning/phases/LT-01-secure-electron-walking-skeleton/01-PATTERNS.md
@.planning/phases/LT-01-secure-electron-walking-skeleton/01-01-SUMMARY.md

<read_first>
- `01-01-SUMMARY.md` complete package approval; halt if any required phrase, package, lifecycle-script disposition, or react-dom decision is absent.
- `docs/DEVELOPMENT.md` exact Node/npm policy and host-tool isolation.
- `01-RESEARCH.md` Standard Stack, Package Legitimacy Audit, and exact install commands.
- `01-VALIDATION.md` one-shot commands, latency sampling, and Wave 0 test gaps.
- `docs/TEST_DATA.md` fixture and output sanitation rules.
</read_first>

<interfaces>
This plan creates stable one-shot executor interfaces for `typecheck`, `test:unit`, `build:renderer`, `test:electron`, `test:security`, `check:privacy`, and `check`. Only typecheck and the minimal unit smoke path are runnable in this plan; Plan 01-03 supplies the referenced renderer, Electron, security, and privacy files. Every test script uses a terminating run command, never watch mode.
</interfaces>
</context>

<source_coverage>
- CONTEXT D-11: no installation begins before the explicit package checkpoint.
- RESEARCH Standard Stack and Package Legitimacy Audit: admit only the exact reviewed packages.
- RESEARCH Validation Architecture: create the smallest runnable RED/GREEN test seam first.
- REQ LT-SEC-001/LT-LIC-001/LT-PRV-001: strict compiler/test behavior, reproducible dependency evidence, and sanitized test output.
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Install the approved graph through one strict ESM smoke path</name>
  <precondition>`git branch --show-current` is `phase/01-secure-electron-walking-skeleton`, Node reports 24.x, and `01-01-SUMMARY.md` records approval of all seven SUS packages, lifecycle scripts, and the react-dom assumption.</precondition>
  <reversibility rating="costly">The exact manifest and lockfile become the dependency contract for every later Phase 1 plan.</reversibility>
  <files>package.json, package-lock.json, tsconfig.json, tests/unit/toolchain-smoke.test.ts</files>
  <behavior>
    - RED: the smoke test cannot run because no reviewed TypeScript package boundary exists.
    - GREEN: runtime dependencies are exactly `electron@41.2.0`, `react@19.1.0`, `react-dom@19.1.0`, and `zod@4.3.6`; development dependencies are exactly `typescript@6.0.2`, `vite@8.1.4`, `vitest@4.1.10`, and `@playwright/test@1.62.1`.
    - The test proves ESM TypeScript execution and emits no environment, path, secret, session, clipboard, or result content.
  </behavior>
  <action>Write the failing smoke test first. Create a private ESM manifest with Node 24 engine metadata and only the approved exact direct packages, then run the two exact `npm install --save-exact` commands from research so the lockfile is generated after the blocking gate and approved lifecycle scripts may run. Do not add Tailwind, electron-builder, better-sqlite3, react-i18next, ESLint, Prettier, sidecar, model, database, networking, telemetry, or updater packages. Add strict TypeScript settings with no implicit any, unchecked indexed-access protection, exact optional properties, and separated browser/Node libraries.</action>
  <verify>
    <automated>npm ls electron@41.2.0 react@19.1.0 react-dom@19.1.0 zod@4.3.6 typescript@6.0.2 vite@8.1.4 vitest@4.1.10 @playwright/test@1.62.1 --depth=0 &amp;&amp; npm run typecheck &amp;&amp; npm run test:unit -- toolchain-smoke</automated>
  </verify>
  <done>The exact graph is locked after approval, the smoke test completes its RED/GREEN cycle, strict typecheck passes, and no unreviewed direct package exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound the one-shot Vitest contract and quick-feedback sample</name>
  <reversibility rating="reversible">Test include/exclude, timeout, and reporter settings can change without altering production interfaces.</reversibility>
  <files>package.json, vitest.config.ts, tests/unit/toolchain-smoke.test.ts</files>
  <behavior>
    - `npm run test:unit` executes once and exits with the underlying Vitest status.
    - Unit discovery is confined to the intended test tree and excludes Electron integration, hardware, generated evidence, and native binaries.
    - Three timed smoke samples are recorded; the median and slowest sample remain below 60 seconds or execution stops for plan revision.
  </behavior>
  <action>Write runner-contract assertions before completing the Vitest configuration. Set deterministic environment, bounded test and hook timeouts, stable sanitized reporting, and explicit include/exclude rules. Define the named stable package scripts with exact one-shot commands; `test:unit` uses `vitest run`, and the renderer/Electron/security/privacy scripts target the files that Plan 01-03 creates. Never enable watch, update snapshots implicitly, or collect hardware/runtime evidence here. Run three one-shot unit samples and record durations in the plan summary without absolute paths or host identity.</action>
  <verify>
    <automated>npm ci &amp;&amp; npm run typecheck &amp;&amp; npm run test:unit -- toolchain-smoke &amp;&amp; npm run test:unit -- toolchain-smoke &amp;&amp; npm run test:unit -- toolchain-smoke</automated>
  </verify>
  <done>A clean install reproduces the exact graph, the runner exits deterministically three times, and the measured quick feedback meets the documented threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to lockfile | Reviewed package bytes and lifecycle scripts enter the local build. |
| test runtime to terminal/summary | Test failures and reporters could disclose host or controlled content. |

## STRIDE Threat Register — ASVS L2

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-LT01-SC | Tampering | npm install and lockfile | high | mitigate | Plan 01-01 dependency and executable precondition block installation until exact identities and lifecycle scripts are approved. |
| T-LT01-02-S | Spoofing | direct dependencies | high | mitigate | Exact package names/versions and `npm ls --depth=0` reject substituted or additional direct packages. |
| T-LT01-02-T | Tampering | compiler/test configuration | medium | mitigate | Strict committed configuration, clean-install reproduction, and smoke assertions. |
| T-LT01-02-I | Information Disclosure | test output | high | mitigate | Content-free fixtures and assertions reject host, secret, path, session, clipboard, and result values. |
| T-LT01-02-D | Denial of Service | unit runner | medium | mitigate | `vitest run`, bounded timeouts, explicit discovery, and three measured one-shot samples. |

Any critical or high finding blocks completion; every medium finding is mitigated under ASVS L2.
</threat_model>

<artifacts>
- Exact private `package.json` and reproducible `package-lock.json`.
- Strict `tsconfig.json` and bounded `vitest.config.ts`.
- Minimal content-free toolchain smoke test.
</artifacts>

<acceptance_criteria>
- No npm install occurs without the Plan 01-01 approval precondition.
- Direct dependencies match the eight-package research set exactly.
- Clean install, strict typecheck, and three one-shot smoke samples pass without watch mode.
- No renderer, security-evidence, privacy-evidence, hardware, application, or native-helper behavior is added in this focused plan.
</acceptance_criteria>

<verification>
Run `npm ci &amp;&amp; npm ls --depth=0 &amp;&amp; npm run typecheck &amp;&amp; npm run test:unit -- toolchain-smoke` and inspect the three recorded durations and sanitized output.
</verification>

<rollback>
Revert only the five listed files. Never repair a rejected or drifted dependency by changing its identity/version outside renewed legitimacy review; regenerate the lockfile only from the still-approved exact manifest.
</rollback>

<completion_evidence>
- Exact `npm ls --depth=0` output.
- Clean-install and strict-typecheck results.
- RED/GREEN smoke result and three sanitized duration samples.
</completion_evidence>

<success_criteria>
The approved package boundary and minimal TDD runner are reproducible before renderer or evidence infrastructure is introduced.
</success_criteria>

<output>
Create `.planning/phases/LT-01-secure-electron-walking-skeleton/01-02-SUMMARY.md` when done.
</output>
