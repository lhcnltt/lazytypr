---
phase: LT-01-secure-electron-walking-skeleton
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements:
  - LT-LIC-001
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "No Phase 1 npm package is installed until every SUS identity is reviewed and explicitly approved."
    - "The react-dom 19.1.0 assumption is either explicitly accepted as React's renderer counterpart or blocks execution for plan revision."
    - "Unexplained lifecycle scripts, license mismatches, repository mismatches, or publish-metadata anomalies block the first install."
  artifacts: []
  key_links:
    - from: "01-RESEARCH.md Package Legitimacy Audit"
      to: "01-02-PLAN.md npm installation"
      via: "blocking-human approval recorded in 01-01-SUMMARY.md"
      pattern: "approved phase1 packages"
---

<objective>
Resolve the Phase 1 supply-chain gate before any npm installation.

Purpose: Enforce the Package Legitimacy Gate for every SUS package and resolve the one unverified package-set assumption without weakening the pinned stack or D-11 delivery gate.
Output: A blocking approval or an explicit rejection that prevents Plan 01-02 from running.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0008-mit-reuse-provenance.md
@docs/LICENSING.md
@docs/DEVELOPMENT.md
@docs/PROVENANCE.yaml
@THIRD_PARTY_NOTICES.md
@.planning/phases/LT-01-secure-electron-walking-skeleton/01-CONTEXT.md
@.planning/phases/LT-01-secure-electron-walking-skeleton/01-RESEARCH.md

<read_first>
- `AGENTS.md` source precedence, invariants, and planning-only boundary.
- `01-CONTEXT.md` D-11 and its accepted-plan/package-gate requirements.
- `01-RESEARCH.md` Standard Stack, Package Legitimacy Audit, Assumptions Log A1, and Environment Availability.
- `docs/DEVELOPMENT.md` exact Phase 1 runtime versions and npm-lock policy.
- `docs/LICENSING.md` separation of dependency licenses from the root MIT license.
</read_first>

<interfaces>
The downstream approval record must name each package, exact version, source repository, license, exact-version publish date, integrity/tarball metadata, and lifecycle-script disposition. It must separately state whether `react-dom@19.1.0` is accepted as the required `createRoot` renderer counterpart to `react@19.1.0`.
</interfaces>
</context>

<source_coverage>
- CONTEXT D-11: accepted plans and configured gates precede implementation.
- RESEARCH Package Legitimacy Audit: all seven SUS packages require a human gate.
- RESEARCH A1: `react-dom` remains an assumption until this checkpoint resolves it.
- REQ LT-LIC-001: dependency identity, license, and provenance are release-blocking obligations.
</source_coverage>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify exact Phase 1 npm identities and resolve react-dom</name>
  <files>None — this is a read-only blocking verification gate.</files>
  <what-built>The research audit identified seven SUS packages whose exact registry identities require human confirmation before the first installation.</what-built>
  <how-to-verify>
1. Open each exact npm page and its linked source repository, then compare `name`, `version`, `repository`, `license`, `dist.integrity`, `dist.tarball`, exact-version publish timestamp, maintainers, and the complete `scripts` object against `npm view PACKAGE@VERSION --json`:
   - `electron@41.2.0` — expected source `github.com/electron/electron`, MIT, published 2026-04-08.
   - `react@19.1.0` — expected source `github.com/facebook/react`, MIT, published 2025-03-28.
   - `react-dom@19.1.0` — expected source `github.com/facebook/react`, MIT, published 2025-03-28.
   - `typescript@6.0.2` — expected source `github.com/microsoft/TypeScript`, Apache-2.0, published 2026-03-23.
   - `vite@8.1.4` — expected source `github.com/vitejs/vite`, MIT, published 2026-07-09.
   - `vitest@4.1.10` — expected source `github.com/vitest-dev/vitest`, MIT, published 2026-07-06.
   - `@playwright/test@1.62.1` — expected source `github.com/microsoft/playwright`, Apache-2.0, published 2026-07-30.
2. For every package, review all install, preinstall, postinstall, prepare, and binary-download behavior. Record why each lifecycle action is expected and acceptable. Any unexplained script, source mismatch, integrity anomaly, license mismatch, typosquat indicator, or unexpected maintainer change rejects the set.
3. Confirm that `react-dom@19.1.0` is accepted as the matching React renderer package needed for `createRoot`, despite not being named in `docs/DEVELOPMENT.md`. If it is not accepted, stop and revise the Phase 1 renderer stack; do not substitute or omit it during execution.
4. Confirm that `zod@4.3.6` remains the research-audited OK package and that no additional direct npm dependency is authorized by this gate.
5. Approve only the complete exact set. Partial approval does not authorize Plan 01-02.
  </how-to-verify>
  <resume-signal>Type `approved phase1 packages` and explicitly state `react-dom assumption accepted` plus `all lifecycle scripts accepted`; otherwise list the rejected package or unresolved metadata.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm metadata to local dependency decision | Untrusted registry/package metadata influences executable code admitted to the project. |
| research assumption to authoritative stack | `react-dom` is technically expected but was not explicitly pinned by the authoritative development document. |

## STRIDE Threat Register — ASVS L2

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-LT01-SC | Tampering | npm package identities | high | mitigate | Blocking-human review of exact identity, version, repository, license, integrity, publish metadata, maintainers, and lifecycle scripts before install. |
| T-LT01-01-S | Spoofing | similarly named npm packages | high | mitigate | Approve only the seven exact scoped names and versions from the research audit. |
| T-LT01-01-R | Repudiation | package approval | medium | mitigate | Record an explicit package-set and react-dom approval in the plan summary consumed by Plan 01-02. |
| T-LT01-01-I | Information Disclosure | public registry inspection | low | accept | Only public package metadata is reviewed; no credentials, local paths, or private data are involved. |

Any critical or high finding blocks approval and therefore blocks every install-dependent plan.
</threat_model>

<artifacts>
- No repository file is created or modified.
- Execution evidence is the explicit approval or rejection captured in `01-01-SUMMARY.md`.
</artifacts>

<acceptance_criteria>
- All seven SUS packages receive the exact identity/version/license/publish/install-script review.
- The `react-dom@19.1.0` assumption is explicitly accepted or execution stops for plan revision.
- No installation, manifest creation, lockfile generation, remote mutation, Git push, release, or tag occurs in this plan.
</acceptance_criteria>

<verification>
Plan 01-02 must refuse to run unless `01-01-SUMMARY.md` contains the complete approval signal; a rejection is a correct blocking outcome, not a bypassable warning.
</verification>

<rollback>
No repository state changes. Withhold or revoke approval and revise the dependency plan if any reviewed metadata changes before installation.
</rollback>

<completion_evidence>
- Human-reviewed package metadata for all seven SUS packages.
- Explicit lifecycle-script dispositions.
- Explicit react-dom assumption resolution.
</completion_evidence>

<success_criteria>
The first npm install is cryptographically and procedurally gated by an unambiguous human approval of the exact package set.
</success_criteria>

<output>
Create `.planning/phases/LT-01-secure-electron-walking-skeleton/01-01-SUMMARY.md` when the gate is resolved.
</output>
